# 新能源物流车队协同调度系统答辩展示稿

> 展示时间：10 分钟  
> 建议展示顺序：先打开 Streamlit 仪表盘或播放 `demo.gif`，再切到项目结构、核心代码和实验结果。

## 一、开场介绍，约 40 秒

各位老师、同学大家好，我们小组展示的项目是“新能源物流车队协同调度系统”。

这个项目面向的是城市新能源物流配送场景。系统中有一批电动车辆、不断动态出现的配送任务、带属性的城市道路网络，以及有限容量的充电站。我们的目标是在车辆电量、载重、体积、货物类型、任务截止时间和充电排队等约束下，为车辆动态分配任务，并比较不同调度策略的效果。

所以它不是一个单纯的最短路问题，也不是一次性的静态任务分配问题，而是一个持续变化的动态调度仿真问题。我们最终做成了一个可以运行、可以可视化、可以做多策略实验对比的平台。

## 二、问题理解，约 2 分钟

我们对题目的理解主要分成四个对象：路网、车辆、任务和充电站。

第一是城市路网。我们把城市道路抽象成图结构，节点表示路口或者关键地点，边表示道路。边上不仅有距离，还包含限速、车道数、道路类型、路面质量等属性。这样车辆行驶时，系统不只是计算两点之间的直线距离，而是基于图结构计算路径、时间、成本和能耗。

第二是车辆。车辆是有限资源，而且是异构的。系统里有小型车、标准车和大型车，它们的电池容量、载重上限、体积上限、最高速度、充电速度和支持货物类型都不同。也就是说，调度时必须先判断一辆车是否真的能执行某个任务。

第三是任务。任务在仿真过程中动态生成，每个任务包含起点、终点、重量、体积、货物类型、创建时间、截止时间和优先级。任务越早完成、路径越短、成本越低，得分越高；如果超时或无法执行，就会被标记为失败。

第四是充电站。新能源车调度和普通燃油车不同，必须考虑电量和补能问题。项目中每个充电站都有有限数量的充电桩，车辆低电量时会前往充电站，如果充电桩已满，就需要排队等待。

因此，我们的核心问题可以概括为：在任务不断到达、车辆状态不断变化、充电资源有限的情况下，如何根据当前系统状态，持续做出较合理的车辆任务分配和充电决策。

## 三、功能实现，约 3 分钟

从系统结构上看，项目采用模块化设计，主要分为五个部分。

`core` 目录负责核心实体和数据结构，包括路网、车辆、任务、能耗模型、充电站和调度接口。

`algorithms` 目录负责调度策略，目前实现了三种策略：最近任务优先、最大任务优先和综合评分策略。

`simulator` 是仿真主循环，负责推进时间、生成任务、调用调度器、执行动作、更新车辆和任务状态，并统计最终结果。

`scenarios` 负责构建不同规模的城市实验场景。用于批量实验的 benchmark 支持 `small_city`、`medium_city`、`large_city` 和 `mega_city` 四种规模；用于现场交互展示的 Streamlit 页面则提供“小规模”“中规模”“大规模”三档轻量预设，方便快速运行和播放动画。

`visualization` 负责结果展示，包括 Matplotlib 动画和 Streamlit 交互式仪表盘。

系统运行流程可以理解为“按小时调度 + 间隔内事件驱动推进”：

首先，在每个调度时刻，仿真器会推进已经到期的事件，包括车辆是否完成配送、是否到达充电站、充电是否完成。然后系统生成新的配送任务，把当前车辆、任务、路网和充电站状态封装成 `SimulationState`。调度器根据这个状态生成动作，例如 `assign_task` 表示分配任务，`go_charge` 表示前往充电站。最后仿真器统一执行这些动作，并在下一个调度时刻之前继续按事件时间推进，同时记录可视化帧。

在调度策略上，我们实现了三种方法。

第一种是 `nearest`，最近任务优先。它对空闲车辆选择距离最近、并且满足电量、载重、体积和货物类型约束的任务。这个策略简单直观，速度快，适合作为基线。

第二种是 `largest`，最大任务优先。它优先处理重量较大的任务。我们的考虑是，大任务通常更挑车辆，如果晚处理，可能只剩小车或者低电量车辆，导致任务失败。

第三种是 `composite`，综合评分策略。它会对每个候选的车辆任务组合计算综合得分，综合考虑基础收益、任务优先级、deadline 紧急程度、容量匹配程度、取货距离、配送距离、行驶时间、能耗、延误惩罚和充电站等待时间。这里还特别考虑了任务完成后车辆是否还能到达充电站，并把“任务能耗 + 到站能耗 + 预留电量”作为可行性约束。它的思想可以概括为：先过滤不可行分配，再在可行分配中选择综合收益最高的组合。

除了调度策略，我们还重点实现了数据结构上的优化。

城市路网使用图结构建模，并对最短路距离、路径、行驶时间和道路指标做缓存，避免调度过程中重复计算相同路径。

充电站等待队列使用 `deque`，车辆入队和出队都是 `O(1)`。正在充电的车辆用字典按车辆编号管理，充电完成时间使用最小堆维护，堆顶就是最早完成充电的车辆，这样不需要每个时间步扫描所有充电车辆。

任务管理中，我们实现了 `TaskPriorityPool`，底层使用 `heapq`，可以按 deadline、重量、优先级加 deadline 等方式组织任务，并使用懒删除机制降低删除开销。

可视化方面，项目有两种展示形式。Matplotlib 动画适合快速播放车辆在路网上移动的过程；Streamlit 仪表盘适合现场展示，可以选择小/中/大三档展示场景、调度策略、仿真步数、随机种子和综合评分预设，也可以查看任务趋势、平均电量趋势、路网快照和三策略对比结果。

## 四、实验结果，约 2 分钟

实验部分我们使用 `benchmark.py` 做多策略、多随机种子的对比。当前最新一次结果是在 `small_city` 场景下，对 `nearest`、`largest` 和 `composite` 三种策略分别使用 42、43、44 三个随机种子运行，每次仿真 24 步，每步生成 4 个任务。

从平均结果看：

| 策略 | 完成率 | 准时率 | 单任务成本 | 单任务里程 | 平均总分 |
| --- | ---: | ---: | ---: | ---: | ---: |
| nearest | 98.61% | 98.59% | 47.68 | 26.40 | 4517.69 |
| largest | 98.26% | 99.30% | 47.07 | 26.28 | 4550.89 |
| composite | 97.92% | 99.29% | 47.64 | 26.43 | 4490.24 |

可以看到，在这个小规模场景下，三种策略都能完成大部分任务，说明系统基本调度闭环是有效的。

`nearest` 的完成率略高，说明在车辆和任务密度比较充足时，最近任务优先可以快速消化任务。但它只看局部距离，可能忽略 deadline、能耗和后续充电风险。

`largest` 在这组实验里的平均总分最高，单任务成本和单任务里程也最低。原因可能是优先处理较重任务减少了后续不可行任务的风险，避免大任务被拖到最后无人可接。

`composite` 在小规模场景下没有明显超过另外两种策略，但它考虑因素更完整，尤其是 deadline、能耗、充电可达性和低电风险。在更复杂的城市规模、更紧张的车辆资源或更高任务密度下，它的优势更可能体现出来。项目中还提供了 `tune_composite.py`，可以通过多随机种子自动搜索综合评分策略的权重，为后续参数优化留出了接口。

所以实验结论不是简单地说某个策略永远最好，而是说明我们的平台能够在同一场景、同一随机种子和统一指标下，对不同调度策略进行可复现比较。

## 五、思考与总结，约 2 分钟

这个项目的特点主要有四点。

第一，它把题目从静态路径规划扩展成了动态调度仿真。任务会不断生成，车辆会执行任务、消耗电量、进入充电站排队，系统状态一直在变化。

第二，它比较完整地体现了数据结构的应用。图用于路网和最短路，字典和集合用于实体快速索引，`deque` 用于充电站 FIFO 排队，最小堆用于充电完成事件，优先队列用于任务池管理。

第三，它支持多策略对比。最近优先、最大任务优先和综合评分策略都通过统一接口接入，后续可以继续扩展新的算法。

第四，它有比较完整的展示闭环。我们不仅能看到最终完成率、总成本、总里程和总得分，也能通过动画和仪表盘观察车辆移动、任务完成和充电过程。

当然，目前系统也有不足。

首先，当前路网仍然是模拟生成的网格城市，还没有接入真实地图和真实交通数据。其次，现有调度策略主要是启发式和贪心式策略，速度快、容易解释，但不能保证全局最优。后续可以加入滚动窗口规划、局部搜索、遗传算法或者强化学习。再次，能耗和充电模型还可以继续细化，比如加入温度、坡度、车辆老化和非线性充电曲线。

总的来说，我们完成了一个面向新能源物流配送的动态调度仿真系统。它以图结构表示城市道路，用车辆、任务和充电站描述配送过程，通过多种调度策略进行任务分配，并用 benchmark 和可视化展示实验结果。这个系统满足了大作业的基本要求，也具备继续扩展为调度算法实验平台的基础。

我的展示到这里，谢谢大家。

## 六、现场展示建议

如果现场只能展示 2 到 3 个画面，建议按这个顺序：

1. 先展示 Streamlit 仪表盘，选择“小规模”或“中规模”和“三策略对比”，让老师看到系统能交互运行。
2. 再展示 Matplotlib 动画或 `demo.gif`，说明车辆不是瞬间完成任务，而是在路网上连续移动。
3. 最后展示 `outputs/benchmarks/summary_latest.csv` 或报告中的实验表格，讲三种策略的对比结果。

可用命令：

```bash
python -m streamlit run ev_dispatch/visualization/streamlit_dashboard.py
```

```bash
python -m ev_dispatch.main --visualize --visualize-strategy composite --steps 20 --tasks-per-step 3
```

```bash
python -m ev_dispatch.benchmark --tag latest
```

## 七、答辩可能问题与回答

### 1. 为什么要用图结构？

因为配送车辆是在道路网络上移动的，节点可以表示路口或地点，边表示道路。用图结构后，可以自然支持最短路、行驶时间估计、路径回放和道路属性聚合，比直接用二维距离更符合城市配送场景。

### 2. 为什么最近优先不是最优？

最近优先只关注当前车辆到任务起点的距离，容易做出局部最优选择。它可能忽略任务 deadline、配送距离、能耗、货物类型和完成任务后是否还能到达充电站，所以在复杂场景下不一定整体最优。

### 3. 综合评分策略为什么没有在当前实验中最高？

当前结果是在 `small_city` 小规模场景下得到的，车辆资源相对充足，简单贪心策略也能完成大部分任务，所以综合策略的优势不一定明显。综合策略的价值在于可扩展性和风险控制，尤其适合任务密度更高、车辆电量更紧张、充电站更拥挤的场景。

### 4. 项目中用到了哪些数据结构？

主要包括图、字典、集合、双端队列、最小堆和任务优先队列。图用于路网，字典和集合用于快速索引和去重，`deque` 用于充电排队，`heapq` 用于充电完成事件和任务优先级管理。

### 5. 充电站为什么要用堆？

因为正在充电的车辆可能很多，仿真每一步都扫描所有车辆会比较低效。使用最小堆后，堆顶始终是最早完成充电的车辆，只需要检查堆顶是否到期，插入和弹出都是 `O(log n)`。

### 6. 后续最想改进什么？

我认为最值得改进的是两点：一是接入真实地图和交通速度数据，让场景更真实；二是加入滚动窗口规划或局部搜索，让调度策略不只考虑当前一步，而是优化未来一段时间内的任务组合。
