# 新能源物流车队协同调度系统项目报告

## 一、项目背景与目标

本项目面向城市新能源物流配送场景，模拟中央仓库管理者在动态任务到达、车辆电量受限、载重受限、充电站存在排队压力的情况下，如何对新能源车辆进行任务分配、路径规划和补能调度。

题目要求系统使用图结构实现道路和寻路，模拟一段时间内动态出现的配送任务，并至少实现两种调度策略。本项目在此基础上进一步实现了多规模城市路网、车辆异构配置、货物类型约束、充电站排队、任务截止时间评分、策略对比实验以及图形化可视化展示。

项目核心目标如下：

1. 使用图结构建模城市道路网络，并支持最短路和行驶时间估计。
2. 模拟动态任务生成、车辆状态变化、任务执行和充电过程。
3. 实现多种调度策略，并比较不同策略在完成率、总收益、成本、里程、电量等指标上的差异。
4. 使用合适的数据结构提升仿真与调度效率，包括邻接图、字典、集合、双端队列、堆和任务优先队列。
5. 提供命令行、Matplotlib 动画和 Streamlit 仪表盘等结果展示方式。

## 二、需求分析

根据大作业要求，系统需要覆盖以下功能：

| 要求 | 本项目实现 |
| --- | --- |
| 有限车队，车辆有电量和载重上限 | `Vehicle` 记录电量、载重、体积、车型和支持货物类型 |
| 动态出现任务 | `Simulator.generate_random_task()` 按时间步生成任务 |
| 任务包含时间、地点和重量 | `Task` 包含创建时间、起终点、重量、体积、截止时间 |
| 越早完成、路径越短评分越高 | `estimate_task_assignment()` 和结果指标中计算成本、得分、延误 |
| 电量不足时寻找充电站 | 调度策略生成 `go_charge` 动作 |
| 考虑充电站排队和负荷 | `ChargingStation` 使用等待队列和充电完成堆 |
| 至少两种调度策略 | 已实现最近优先、最大任务优先、综合评分三种策略 |
| 至少三种规模场景 | 已实现 small、medium、large、mega 四种城市规模 |
| 图形界面展示加分 | Matplotlib 动画和 Streamlit Dashboard |

## 三、系统总体设计

项目采用模块化结构：

```text
ev_dispatch/
├── algorithms/      调度策略
├── core/            核心数据结构与实体模型
├── scenarios/       场景规模与默认城市构造
├── simulator/       仿真主循环
├── visualization/   可视化模块
├── benchmark.py     批量实验入口
└── main.py          命令行演示入口
```

系统运行流程如下：

1. 构建城市道路图、车辆列表和充电站列表。
2. 每个仿真时间步生成若干新任务。
3. 将当前状态封装为 `SimulationState`。
4. 调度器根据车辆、任务、道路和充电站状态生成动作。
5. 仿真器执行动作，包括任务分配、车辆运输、前往充电站和充电。
6. 记录每一帧 `SimulationFrame`，供可视化播放。
7. 输出完成率、总里程、总成本、总得分、平均电量、充电等待时间等指标。

## 四、核心数据结构设计

### 4.1 图结构：城市道路网络

道路网络由 `RoadNetwork` 表示，底层是无向加权图。节点表示城市路口或关键位置，边表示道路。

每条边包含以下属性：

- `length_km`：道路长度
- `road_type`：道路类型
- `lane_count`：车道数
- `speed_limit_kmph`：限速
- `surface_quality`：路面质量
- `slope_grade`：坡度
- `intersection_density`：路口密度
- `peak_intensity`：高峰拥堵强度
- `toll_per_km`：道路收费
- `accident_risk`：事故风险
- `truck_restriction`：货车限制

道路图支持：

- 最短距离查询
- 考虑拥堵的行驶时间估计
- 路径节点序列获取
- 带时间进度的路径获取
- 路径属性聚合，例如能耗因子、收费、风险成本

项目中对最短路结果进行了缓存，包括：

- `_nearest_node_cache`
- `_distance_cache`
- `_distance_path_cache`
- `_time_path_cache`
- `_time_length_cache`
- `_road_metrics_cache`

缓存使用字典实现，可以避免在调度过程中反复计算相同的最短路。

### 4.2 字典与集合：实体快速索引

系统大量使用字典和集合来提升状态查询效率：

- `charging_vehicles: Dict[str, ChargingRecord]`：按车辆编号查询正在充电的记录。
- `_completed_task_ids`、`_failed_task_ids`：用集合避免任务重复计入完成或失败列表。
- `planned_loads`、`planned_volumes`：调度时记录车辆计划载重，避免重复分配导致超载。
- `vehicle_details`、`task_details`、`station_details`：可视化帧中的对象快照。

### 4.3 充电站队列：deque + heapq

原始等待队列若使用普通 `list`，从队头出队需要 `pop(0)`，时间复杂度为 `O(n)`。优化后使用 `collections.deque`：

```python
waiting_queue: Deque[str]
```

入队 `append()` 和出队 `popleft()` 均为 `O(1)`。

正在充电车辆使用字典保存：

```python
charging_vehicles: Dict[str, ChargingRecord]
```

同时使用最小堆维护充电完成时间：

```python
charging_finish_heap: List[Tuple[datetime, str]]
```

堆顶始终是最早完成充电的车辆。仿真推进时，只需要不断弹出堆顶中已经到期的记录即可，避免每个时间步扫描所有正在充电车辆。

复杂度对比：

| 操作 | 原方式 | 优化后 |
| --- | --- | --- |
| 等待队列出队 | `O(n)` | `O(1)` |
| 查找到期充电车辆 | `O(k)` 扫描 | `O(log k)` 弹出 |
| 查询车辆是否在充电 | `O(1)` | `O(1)` |

### 4.4 任务池：优先队列

项目新增 `TaskPriorityPool`，使用 `heapq` 实现任务优先队列。它支持三种排序策略：

| 策略 | 含义 |
| --- | --- |
| `deadline` | 截止时间越早优先级越高 |
| `weight` | 重量越大优先级越高 |
| `priority_deadline` | 任务优先级高、截止时间早的任务优先 |

任务池采用懒删除机制。当任务已被分配时，只标记其 `task_id` 已移除，真正弹出堆顶时再清理无效项。这样可以避免在堆中间删除元素造成额外开销。

任务池用于：

- 最近优先策略中按 deadline 遍历候选任务。
- 最大任务优先策略中直接从 weight 堆弹出任务。
- 综合评分策略中按 priority/deadline 生成候选任务集合。

## 五、调度算法设计

### 5.1 最近任务优先策略

最近任务优先策略的基本思想是：对每辆空闲车辆，优先选择距离当前位置最近且满足容量、电量、货物类型约束的任务。

主要步骤：

1. 筛选空闲且电量充足的车辆。
2. 如果车辆电量低于阈值，则优先生成前往充电站动作。
3. 对车辆和任务进行匹配估计。
4. 选择取货距离最短的可行匹配。
5. 生成 `assign_task` 动作。

该策略简单、速度快，但可能忽略任务截止时间和整体收益。

### 5.2 最大任务优先策略

最大任务优先策略使用 `TaskPriorityPool(policy="weight")`，优先处理重量较大的任务。

适用场景：

- 大重量任务更难被车辆承载。
- 如果不提前处理，后续可能只剩小车或低电车辆，导致大任务失败。

该策略可以提高大件任务的处理概率，但可能牺牲部分短距离任务收益。

### 5.3 综合评分策略

综合评分策略在每个候选车辆-任务组合上计算综合得分，考虑因素包括：

- 基础任务收益
- 任务优先级
- 截止时间紧急程度
- 车辆容量匹配程度
- 取货距离
- 送货距离
- 行驶时间
- 能耗
- 延误惩罚
- 充电站等待时间
- 任务完成后到充电站的可达性
- 低电量风险

综合评分公式可概括为：

```text
score =
  base_reward
  + priority_bonus
  + urgency_bonus
  + load_fit_bonus
  - pickup_distance_penalty
  - delivery_distance_penalty
  - travel_time_penalty
  - energy_penalty
  - lateness_penalty
  - charging_wait_penalty
  - low_battery_penalty
```

该策略比前两种贪心策略更全面，也更适合复杂场景。

## 六、路径、能耗与评分模型

### 6.1 路径规划

车辆执行任务分为两段路径：

1. 车辆当前位置到任务起点。
2. 任务起点到任务终点。

车辆前往充电站也会生成独立的充电行驶路径。可视化中 `phase="to_charge"` 表示车辆正在前往充电站，而不是瞬间移动到站点。

### 6.2 能耗模型

能耗由 `EnergyManager.calculate_consumption()` 计算，主要受以下因素影响：

- 行驶距离
- 载重
- 速度
- 车辆基础能效
- 道路能耗因子

当车辆电量不足以完成任务或到达充电站时，系统会拒绝该任务分配或安排补能。

### 6.3 评分模型

任务评分考虑距离、时间、费用和是否超时。任务越早完成、路线越短、费用越低，综合收益越高；若超时未完成，则任务失败并扣分。

最终结果指标包括：

- 完成任务数
- 失败任务数
- 任务完成率
- 准时率
- 平均延误时间
- 总里程
- 总成本
- 总得分
- 平均电量比例
- 充电车辆数
- 平均充电等待时间

## 七、仿真系统设计

仿真器 `Simulator` 是系统主循环，负责统一推进车辆、任务和充电站状态。

每个时间步的流程：

1. 推进前往充电站的车辆，到达后加入充电站队列。
2. 处理已完成充电的车辆。
3. 从充电站等待队列中拉起车辆开始充电。
4. 处理已经完成运输任务的车辆。
5. 将超时未分配任务标记为失败。
6. 生成新任务。
7. 调度器生成动作。
8. 执行动作。
9. 生成若干可视化帧。

为了让动画更平滑，每个仿真时间步还会生成多个子帧，展示车辆沿路径连续移动。

## 八、场景规模设计

项目支持四种规模场景：

| 场景 | 路网节点 | 车辆数 | 充电站数 | 仿真步数 | 每步任务数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| small_city | 64 | 12 | 4 | 24 | 4 |
| medium_city | 144 | 32 | 10 | 36 | 11 |
| large_city | 256 | 90 | 24 | 48 | 30 |
| mega_city | 400 | 220 | 60 | 72 | 72 |

不同规模用于测试算法在路网复杂度、车辆规模和任务压力提升时的表现。

## 九、可视化设计

项目提供两种可视化方式。

### 9.1 Matplotlib 动画

Matplotlib 动画用于快速展示车辆、任务、充电站和道路网络状态。车辆颜色表示电量，任务点表示待处理、执行中或失败状态。

运行方式：

```bash
python -m ev_dispatch.main --visualize --steps 20 --tasks-per-step 3
```

### 9.2 Streamlit 仪表盘

Streamlit Dashboard 支持：

- 选择场景规模
- 选择调度策略
- 设置仿真步数和任务生成速度
- 三策略对比
- 播放车辆轨迹动画
- 查看车辆、任务、道路和充电站详情
- 查看任务状态趋势和平均电量趋势

运行方式：

```bash
python -m streamlit run ev_dispatch/visualization/streamlit_dashboard.py
```

## 十、实验设计与结果分析

### 10.1 实验方法

使用 `benchmark.py` 运行多规模、多策略、多随机种子的对比实验：

```bash
python -m ev_dispatch.benchmark
```

可自定义实验：

```bash
python -m ev_dispatch.benchmark --scales small_city,medium_city --strategies nearest,largest,composite --seeds 42,43,44
```

实验记录指标包括：

- `completion_rate`
- `on_time_rate`
- `avg_delay_hours`
- `total_distance`
- `total_cost`
- `total_score`
- `avg_battery_ratio`
- `avg_station_wait_minutes`

### 10.2 策略对比分析

最近任务优先策略通常路径较短，适合任务分布较均匀、车辆数量充足的情况。

最大任务优先策略更关注难处理的大重量任务，适合重货任务比例较高的情况，但可能增加总里程。

综合评分策略综合考虑时间、距离、能耗、截止时间和充电风险，在复杂场景中更稳定。其缺点是计算量更大，因为需要评估更多车辆-任务组合。

### 10.3 数据结构优化效果

本项目对两个关键结构进行了优化：

1. 充电站等待队列由 `list` 改为 `deque`。
2. 充电完成检测由遍历字典改为最小堆。
3. 待处理任务由普通列表改为任务优先队列。

优化意义：

- 当充电车辆数增多时，堆结构能更快找到最早完成充电的车辆。
- 当任务池较大时，优先队列能直接按策略取出高优先级任务。
- 数据结构与调度策略解耦，使后续添加 EDF、收益密度优先、紧急任务优先等策略更方便。

## 十一、复杂度分析

设：

- `V` 为路网节点数
- `E` 为路网边数
- `N` 为车辆数
- `M` 为待处理任务数
- `C` 为充电站中正在充电车辆数

主要操作复杂度：

| 操作 | 复杂度 |
| --- | --- |
| Dijkstra 最短路 | `O((V + E) log V)` |
| 任务池插入 | `O(log M)` |
| 任务池弹出 | `O(log M)` |
| 充电等待队列入队 | `O(1)` |
| 充电等待队列出队 | `O(1)` |
| 充电完成堆插入 | `O(log C)` |
| 充电完成堆弹出 | `O(log C)` |
| 最近优先单轮匹配 | 约 `O(NM * path_cost)` |
| 综合评分单轮匹配 | 约 `O(NM * estimate_cost)` |

其中 `path_cost` 和 `estimate_cost` 受最短路查询影响，但项目中使用了缓存来减少重复计算。

## 十二、项目特色与创新点

1. 不只是静态最短路问题，而是包含动态任务、车辆状态、充电排队和任务截止时间的综合调度仿真。
2. 使用图结构建模城市路网，并将道路类型、拥堵、风险、收费等因素纳入路径估计。
3. 使用 `deque + heapq` 优化充电站排队和完成事件处理。
4. 使用任务优先队列支持不同调度策略。
5. 支持四种城市规模和多随机种子 benchmark。
6. 提供 Streamlit 交互式可视化，能观察车辆行驶、任务执行和充电过程。
7. 支持车辆前往充电站的连续轨迹动画，而非瞬间移动。

## 十三、不足与改进方向

当前系统仍有改进空间：

1. 可进一步实现自定义 Dijkstra 和 A*，减少对外部图库的依赖，更突出数据结构课程特点。
2. 当前调度策略主要是贪心策略，可以加入滚动窗口规划、局部搜索或遗传算法。
3. 可以支持多车协同完成同一个超大任务。
4. 可以将仿真主循环升级为完全事件驱动，用事件堆处理任务生成、车辆到达和充电完成。
5. 可以进一步加入真实地图数据、实际交通速度数据和更精细的充电模型。

## 十四、总结

本项目完成了一个较完整的新能源物流车队动态调度仿真系统。系统以图结构表示城市路网，以车辆、任务和充电站为核心实体，通过多种调度策略完成动态任务分配和路径规划。

从数据结构角度看，项目使用了图、字典、集合、双端队列、最小堆和任务优先队列等结构，并将它们应用于最短路查询、任务管理、车辆状态维护和充电站排队等关键场景。通过多规模 benchmark 和可视化展示，项目能够较直观地比较不同策略的效果，也能够说明合适数据结构对仿真效率和系统可扩展性的作用。

总体而言，本项目满足大作业的基本要求，并在调度策略、充电排队、场景规模和图形化展示方面进行了扩展。
